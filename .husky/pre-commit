#!/bin/sh

echo "🔍 Running pre-commit checks..."

# Format + lint staged files only. Full lint/typecheck runs in CI.
pnpm lint-staged

# Generate Prisma client so API types stay in sync with schema changes
pnpm --filter api exec prisma generate

echo "✅ Pre-commit checks passed"
