#!/bin/sh

echo "🔍 Running pre-commit checks..."

# Format staged files
echo "✨ Formatting staged files..."
pnpm lint-staged

# Run linting (full turbo lint; type-check runs in CI to keep hook under ~1m)
echo "🔎 Running linter..."
pnpm lint

echo "✅ All checks passed!"
