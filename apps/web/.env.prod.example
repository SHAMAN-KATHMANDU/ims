# Web Production / Staging Environment Variables
# Set these at build time (e.g. in CI or Docker build). Required in non-dev.

# Required in staging and production (build will throw if missing)
NEXT_PUBLIC_API_URL=https://your-api-domain.com/api/v1

# Environment (set by host or build; do not default to development in prod)
NODE_ENV=production
