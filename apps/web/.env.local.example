# For local dev, use same-origin proxy (no CORS): Next.js rewrites /api/v1 to the API.
# Create .env.local with:
#   NEXT_PUBLIC_API_URL=/api/v1
# If you call the API directly instead: NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
