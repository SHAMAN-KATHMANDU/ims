import { NavigationEditorView } from "@/features/tenant-site/navigation";

export const metadata = { title: "Navigation — CMS" };

export default function NavigationPage() {
  return <NavigationEditorView />;
}
