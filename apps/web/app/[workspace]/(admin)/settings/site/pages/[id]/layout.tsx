import type { ReactNode } from "react";

export const metadata = { title: "Edit page" };

export default function EditTenantPageLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
