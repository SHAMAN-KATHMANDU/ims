/**
 * Backward-compat re-export — blueprints have been split into individual
 * template modules under templates/. This file maintains the original
 * import paths for existing consumers (sites.service.ts, site-layouts.service.ts).
 */

export * from "./templates";
